/** Get sale items */
export const getProducts = (state) => state.products;

/** Get the loading state of restock */
export const getIsLoading = (state) => state.is_loading;

export const getTotalPages = (state) => state.total_pages;